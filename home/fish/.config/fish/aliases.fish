# git aliases
alias g   'git'
alias ga  'git add'
alias gc  'git commit'
alias gd  'git diff'
alias gds 'git diff --staged'
alias gp  'git push'
alias gs  'git status'

# ls aliases
alias lsa 'ls -la'
